// Replaced by the shared `components/shared/TxProgress` component. This file
// is intentionally empty to avoid a dangling import during the rename; delete
// the file entirely once no tooling references it.
export {}
